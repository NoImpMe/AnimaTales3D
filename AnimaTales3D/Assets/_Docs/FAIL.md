# FAIL.md — 과거 실패 목록

같은 실수를 반복하지 않기 위한 기록. 형식: `- [날짜] 증상 → 원인 → 방지 규칙`

- [2026-09-02] .gitignore를 프로젝트 루트(`AnimaTales2D/`) 기준으로 고쳤는데도 이미 스테이징돼 있던 대량 파일(Library/Temp/Obj 등)이 계속 스테이징 상태로 남음 → gitignore는 "새로 add되는 파일"만 막고, 이미 인덱스에 올라간 파일은 자동으로 안 빠짐 → 프로젝트 폴더 구조를 옮기거나(예: `My project/` 중간 폴더 제거) gitignore 기준 경로를 바꾼 뒤에는 `git rm -r --cached <경로>`로 기존 인덱스에서 명시적으로 제거하고, `git status --short`로 재확인할 것
- [2026-09-02] `manage_script`의 `create` 액션이 기존 파일 경로에 대해 실패("Script already exists... Use 'update' action to modify.")했으나 실제 `action` enum에는 `update`가 없음(create/read/delete만 존재) → 도구 설명이 실제 기능과 불일치 → `delete` 후 `create`로 재생성해서 우회. 이때 `.meta`(스크립트 GUID)는 delete로 지워지지 않고 그대로 남아 씬의 `MonoBehaviour` 참조가 깨지지 않음을 확인함(재생성 전후 guid 대조로 검증) → 앞으로도 기존 스크립트를 통째로 교체할 때는 delete→create 패턴을 쓰되, 매번 재생성 후 `.meta` guid를 씬/프리팹 참조와 대조해 확인할 것
- [2026-09-05] `unity command set_serialized_field`/`get_component_properties` 등의 파라미터 이름을 추측(`--path`, `--component` 등)했다가 `INVALID_COMMAND_ARGS`로 실패 → CLI 명령마다 파라미터 스키마가 다르고 이름이 직관과 다를 수 있음(`path`가 아니라 `field`) → 처음 쓰는 `unity command <이름>`은 실행 전에 `unity command --format json`으로 해당 명령의 `parameters` 스키마를 먼저 확인할 것
- [2026-09-05] `unity command instantiate_prefab`은 `.prefab` 자산만 허용하고 FBX 모델 프리팹은 "not a prefab asset"으로 거부함 → FBX를 씬에 배치할 때는 `eval`로 `PrefabUtility.InstantiatePrefab(AssetDatabase.LoadAssetAtPath<GameObject>(fbxPath))`을 직접 호출할 것
- [2026-09-05] `eval` 코드에서 `UnityEngine.Object.GetInstanceID()`를 쓰면 이 프로젝트의 Unity 버전(6000.5)에서 obsolete 경고가 컴파일 에러로 처리되어 명령 자체가 실패함 → 로그/식별용으로는 이름(`.name`) 등 다른 값을 사용할 것
- [2026-09-05] Play 모드에서 Unity 에디터 창이 포커스를 잃으면 Player Loop(`Time.frameCount`)가 완전히 멈춰서, CLI로 `editor_play` 후 `eval`만으로는 코루틴/애니메이션 같은 시간 기반 동작의 "완료"까지는 관찰할 수 없음(트리거 직후 상태 변화는 확인 가능하나 그 이후가 안 됨) → 이런 경우 육안 확인이 필요하다는 점을 완료 보고에 명시할 것. `SetForegroundWindow`/`AppActivate`로 포커스를 다시 줘도 이미 멈춘 프레임 루프가 재개되지 않았음(재현성 미확인)
- [2026-09-05] `Sprite-Lit-Default`(URP 2D) 셰이더를 쓰는 SpriteRenderer의 투명 배경이 `editor_play` 직후 3초 이내 `capture_game_view`로 캡처하면 흰 사각형으로 보임 → 셰이더/GPU Instancing/Particle/좌표계/C# 상태를 전부 배제하고 나서야 "Play 진입 직후 셰이더 워밍업이 안 끝난 에디터 한정 지연 현상"으로 확인(10초 대기 후 재캡처하면 정상). → 2D Lit 셰이더+3D 씬 조합을 스크린샷으로 검증할 때는 최소 5~10초 대기 후 캡처할 것. 또한 `SpriteRenderer`는 `renderer.material.color`가 아니라 `SpriteRenderer.color` 전용 프로퍼티를 써야 함(material.color는 히든 `_Color`를 건드려 알파 블렌딩이 깨질 수 있음)
