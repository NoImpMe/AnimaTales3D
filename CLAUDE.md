# 프로젝트: 2D 턴제 전투 로그라이트 게임 → 3D 전환

Unity 2D 턴제 전투 로그라이트 게임(헥사타일 스테이지 기반) → 3D 전환 프로젝트. 개발자 1인.

## 기본 원칙

- 전환 작업에서는 기존 기능(턴제 전투 규칙, 헥사타일 좌표 로직 등) **보존을 최우선**으로 한다.
- 전환과 무관한 스코프 확장은 자제한다. 확장은 전환이 끝난 뒤 별도로 논의한다.
- Unity CLI(`unity` 명령)로 에디터를 직접 조작해야 하는 경우는 사람에게 먼저 물어본다.
- 코드 제안 후 실행이 기본이다.
- git 명령은 Bash로 직접 실행한다.
- Unity 에디터 조작 전에는 `unity status`로 연결된 에디터가 있는지 먼저 확인한다.

## 파일 읽기

`[조사]` `[설계]` `[구현]` `[수정]` `[복구]` `[전환]` 명령을 받으면, 지시 없이도 먼저 아래를 읽는다.

1. `Assets/_Docs/FAIL.md` — 과거 실패 목록. 같은 실수 반복 금지.
2. `Assets/_Docs/CONVERSION_SPEC.md` — 2D→3D 전환 사양: 좌표계 매핑 규칙, 카메라/조작 설계, 보존해야 할 기존 로직 범위.

- `.md` 파일은 Read 도구로 직접 읽는다.
- 단순 질문, 개념 설명, 잡담에는 파일을 읽지 말고 바로 답한다.
- 읽은 뒤 아래 3줄로 보고한 다음 작업에 들어간다.
  - 현재 단계:
  - 대기 중:
- 파일이나 폴더가 없으면 임의로 만들지 말고, 없다고 보고한 뒤 지시를 기다린다.
  - 단, `[조사]`와 `[설계]`는 문서가 없어도 그대로 진행한다. 수정이 없기 때문이다.
- Unity가 연결되지 않았으면 그 사실만 알리고 멈춘다.

## 동사 규약

| 동사 | 의미 |
|---|---|
| `[조사]` | 현재 구조·원인 파악. 파일을 수정하지 않는다. |
| `[설계]` | 방안 2~3개 제시. 실행하지 않는다. |
| `[구현]` | 새 기능 추가. |
| `[수정]` | 기존 동작 변경·버그 픽스. |
| `[전환]` | 기존 2D 로직/에셋을 3D로 마이그레이션한다. 변환 전후 동작(턴제 규칙, 헥사타일 좌표 연산 결과)이 같은지 비교 검증을 포함한다. |
| `[복구]` | 진단만 한다. 원인 가설 3개와 검증 방법을 제시하고 멈춘다. 고치지 않는다. |

## LOG.md 기록 형식

```
## [동사] 한 줄 목표 — YYYY-MM-DD HH:MM
### 프롬프트
(내가 준 명령 원문을 요약하거나 다듬어라)
### 조작 내역
(호출한 Unity CLI 명령(`unity command <이름>`)과 대상 파일. [설계]는 제시한 방안 요약)
### 검증
(실행한 테스트·명령과 실제 출력. [설계]는 "해당 없음")
### 실패와 수정
(있으면 기록. 없으면 "없음")
```

- git commit까지 실행한다. 커밋 없이 완료 보고를 하지 마라.
- git push는 하지 마라. 사람이 직접 한다.
- 명령 1개 = 커밋 1개 = LOG 항목 1개. 이 대응을 깨지 마라.
- 같은 실수가 반복되면 `FAIL.md`에 증상·원인·방지 규칙을 한 줄 추가한다.

## 커밋 메시지 규약

```
<type>: <목표 한 줄>
명령: <내가 준 명령의 첫 줄>
검증: <실행한 테스트와 결과>
LOG: LOG.md #<항목 번호>
```

`type` 매핑:

| 작업 | type |
|---|---|
| `[설계]` | docs |
| `[구현]` | feat |
| `[수정]` 버그 | fix |
| `[수정]` 구조 | refactor |
| `[전환]` | convert |
| 테스트 | test |
| 문서 | docs |
| 에셋 | art |
| 빌드·설정·체크포인트 | chore |

커밋 메시지를 임의 형식으로 쓰지 마라.

## 작업 방식

아래 1~8번은 `[구현]` `[수정]` `[전환]`에만 적용한다.
`[설계]`와 `[복구]`는 결과를 보고한 뒤 9~10번만 수행한다.

1. 작업 시작 전 Bash로 체크포인트 커밋을 직접 실행한다.
   메시지: `chore: checkpoint YYYY-MM-DD HH:MM`
   사람에게 커밋을 요청하지 마라. 에디터 메뉴에 의존하지 마라.
2. 순수 로직을 MonoBehaviour 밖 C# 클래스로 먼저 쓰고 EditMode 테스트를 붙인다.
   `[전환]`의 경우, 기존 2D 좌표/로직의 결과값과 3D 매핑 결과를 비교하는 회귀 테스트를 반드시 포함한다.
3. 그 다음 MonoBehaviour와 씬에 연결한다.
4. `unity command recompile`로 컴파일을 트리거하고, `unity command recompile_status`가 `completed`/`up_to_date`가 될 때까지 폴링해 대기한다. 대기 없이 다음 명령을 보내지 않는다.
5. `unity command console --level error` (또는 `get_console_logs`)로 Error/Exception 0건을 확인한다.
6. `unity command run_tests`로 테스트를 실행한다 (`mode` 파라미터로 EditMode/PlayMode 지정).
7. `unity command save_scene` (또는 `save_all`)로 씬을 명시적으로 저장한다.
8. 변경 파일 목록을 확인한다.
9. `LOG.md`에 기록한다.
10. `git commit`을 실행한다.

- 모든 수치는 ScriptableObject Config가 소유한다. MonoBehaviour에 숫자 리터럴 금지.
- 안전하게 추론 가능한 세부는 스스로 결정하고 진행한다.
- 단계마다 확인을 구하지 않는다. 범위나 SPEC을 바꾸는 결정만 묻는다.
- 완료 보고에는 실행한 검증 명령과 실제 출력, 변경 파일 목록, 그리고 내가 눈으로 봐야 판정되는 항목을 분리해서 적는다.

## 절대 하지 않는다

- 패키지 추가·제거, Project Settings 변경
- git push, git reset, git checkout, git revert, force push — 사람만 실행한다
- 회귀 검증 없이 기존 2D 로직·씬을 삭제하는 것 — 전환 완료 확인 후에만 정리한다

## 참고 문서

- `Assets/_Docs/FAIL.md`
- `Assets/_Docs/CONVERSION_SPEC.md`
- `claude/헥사타일_3D전환_지침.md`
- `claude/AnimaTales2D_스크립트_요약.md`
